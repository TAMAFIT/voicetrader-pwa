import assert from 'node:assert/strict';
import { createAdaptiveState,buildAdaptiveSignal,updateAdaptiveState,buildPostMortem } from '../src/short-horizon/adaptive-experiment.js';
const end=Date.parse('2026-08-22T00:04:55Z');const window={windowId:'w',symbol:'BTC/USD',windowSec:5,endTimestampMs:end,timing:{boundary:{'5m':{secondsToBoundary:5,secondsSinceBoundary:295,atBoundary:false}}},price:{closeMid:100,micropriceMinusMidLast:.1},orderFlow:{ofiNormalizedByMeanDepth:.2,depthImbalanceLast:.3},trades:{signedNotionalSum:100}};
const adaptive=createAdaptiveState();const signal=buildAdaptiveSignal(window,adaptive,{generatedAtMs:end});assert.equal(signal.decision,'LONG');assert.equal(signal.evidence.adaptiveStateVersion,0);const loss={outcomeId:'o',experimentId:'BOUNDARY_ADAPTIVE_V1',horizonSec:15,marketReturnBps:-5,result:'LOSS',directionalReturnBps:-5,governance:{signalImmutable:true}};updateAdaptiveState(adaptive,signal,loss);assert.equal(adaptive.stateVersion,1);assert.ok(adaptive.features.OFI_NORMALIZED.weight<1);const pm=buildPostMortem(signal,loss);assert.equal(pm.diagnostics.classification,'DIRECTIONAL_MISS');assert.equal(pm.governance.descriptiveNotCausal,true);console.log('v0.60 adaptive/post-mortem core tests PASS');
