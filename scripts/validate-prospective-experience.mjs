import assert from 'node:assert/strict';
import fs from 'node:fs';
const read=path=>fs.readFileSync(path,'utf8');const pkg=JSON.parse(read('package.json'));const core=read('src/research/prospective-experience-dataset.js');const remote=read('src/research/prospective-experience-remote.js');const builder=read('scripts/build-prospective-experience.mjs');const workflow=read('.github/workflows/prospective-experience-builder.yml');const pwa=read('src/pwa.js');const sw=read('sw.js');
const [major,minor]=String(pkg.version||'0.0.0').split('.').map(Number);assert.equal(major,0);assert.ok(minor>=25,`Prospective Experience Dataset requires package >=0.25.x, got ${pkg.version}`);
for(const marker of ["PROSPECTIVE_EXPERIENCE_DATASET_VERSION='prospective-experience-dataset-0.1'",'PROSPECTIVE_EXPERIENCE_HORIZONS=[1,3,6]','labelsDerivedAfterDecision:true','labelsNotDecisionInputs:true','inputMutationForbidden:true','maturedLabelMutationForbidden:true','pendingLabelsMayMatureOnly:true','noTraining:true','noFitting:true','pooledPerformanceScore:false','usedByDecisionEngine:false','decisionAvailabilityOffsetSeconds:H'])assert.ok(core.includes(marker),`missing dataset marker ${marker}`);
for(const marker of ['frozen-input-mutation','matured-label-mutation','label-regression','source-health-failure','btc-market-archive-mismatch','eth-six-source-decision-set-incomplete'])assert.ok(core.includes(marker),`missing dataset protection ${marker}`);
assert.ok(remote.includes("PROSPECTIVE_EXPERIENCE_DATA_BRANCH='prospective-experience-data'"));assert.ok(remote.includes("PROSPECTIVE_EXPERIENCE_DATA_PATH='data/prospective-experience-v1.json'"));assert.ok(remote.includes('audit?.pass!==true'));assert.ok(builder.includes('buildProspectiveExperienceSnapshot'));assert.ok(builder.includes('auditProspectiveExperienceDataset'));
assert.ok(workflow.includes("cron: '57 * * * *'"));for(const branch of ['knowledge-forward-data','higher-timeframe-forward-data','eth-forward-data','prospective-experience-data'])assert.ok(workflow.includes(`ref: ${branch}`),`missing source branch ${branch}`);for(const source of ['_knowledge/data/knowledge-forward-001.json','_htf/data/htf-forward-001.json','_eth/data/eth-forward-001.json','_experience/data/prospective-experience-v1.json'])assert.ok(workflow.includes(source),`missing builder path ${source}`);assert.ok(workflow.includes("git add -- data/prospective-experience-v1.json"));
assert.ok(pwa.includes("import { setupProspectiveExperienceUI } from './research/prospective-experience-ui.js';"));assert.ok(pwa.includes('setupProspectiveExperienceUI();'));assert.ok(sw.includes('v0.25-prospective-experience'));assert.ok(sw.includes('prospective-experience.css'));assert.ok(sw.includes('prospective-experience-ui.js'));assert.ok(sw.includes('prospective-experience-dataset.js'));assert.ok(sw.includes('prospective-experience-remote.js'));
assert.ok(pkg.scripts.test.includes('validate-prospective-experience.mjs'));assert.ok(pkg.scripts.test.includes('test-prospective-experience.mjs'));for(const path of ['src/engine/shadow-engine.js','src/live/live-forward-paper.js','src/research/knowledge-forward-runner.js','src/research/higher-timeframe-forward-runner.js','src/research/eth-forward-runner.js'])assert.ok(!read(path).includes('prospective-experience'),`${path} must not import Experience Dataset`);
console.log(`Prospective Experience Dataset v0.25 integrity validation passed under package ${pkg.version}.`);
