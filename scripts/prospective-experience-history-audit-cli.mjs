import fs from 'node:fs';
import { auditProspectiveExperienceHistory } from '../src/research/prospective-experience-history-audit.js';
const env=process.env;const required=name=>{const value=String(env[name]||'').trim();if(!value)throw new Error(`missing-env:${name}`);return value;};const manifest=JSON.parse(fs.readFileSync(required('HISTORY_MANIFEST_PATH'),'utf8'));const snapshots=manifest.map(item=>({commitSha:item.commitSha,committedAt:item.committedAt,document:JSON.parse(fs.readFileSync(item.path,'utf8'))}));const audit=auditProspectiveExperienceHistory(snapshots);console.log(JSON.stringify(audit,null,2));if(env.GITHUB_OUTPUT){fs.appendFileSync(env.GITHUB_OUTPUT,`status=${audit.status}\npass=${audit.pass?'true':'false'}\nchecked_commits=${audit.totals.checkedCommits}\nchecked_transitions=${audit.totals.checkedTransitions}\nnew_rows=${audit.totals.newRows}\nmaturations=${audit.totals.maturations}\nexecution_authorized=false\n`);}
