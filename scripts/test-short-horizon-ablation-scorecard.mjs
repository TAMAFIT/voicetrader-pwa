import assert from 'node:assert/strict';import { createScorecardState,updateScorecardState,buildScorecardSnapshot,ABLATION_EXPERIMENT_IDS } from '../src/short-horizon/prospective-scorecard.js';
const s=createScorecardState(),base={symbol:'BTC/USD',horizonSec:15,evaluationPartition:{role:'BLIND_EXAM'},provenance:{inputWindowId:'w'},decision:'LONG'};updateScorecardState(s,{...base,outcomeId:'p',experimentId:'BOUNDARY_CONCORDANCE_V1',sampleRole:'PRIMARY',directionalReturnBps:3,result:'WIN',quotedExecution:{quotedDirectionalReturnBps:2},quotedResult:'WIN'});for(let i=0;i<ABLATION_EXPERIMENT_IDS.length;i++)updateScorecardState(s,{...base,outcomeId:`a${i}`,experimentId:ABLATION_EXPERIMENT_IDS[i],sampleRole:'ABLATION',directionalReturnBps:2-i,result:i<3?'WIN':'LOSS',quotedExecution:{quotedDirectionalReturnBps:1-i},quotedResult:i<2?'WIN':'LOSS'});const snap=buildScorecardSnapshot(s);const ofi=snap.comparisons.primaryVsAblations.BOUNDARY_ABLATE_OFI_V1.BLIND_EXAM;assert.equal(ofi.pairedSamples,1);assert.equal(ofi.meanMidDirectionalDeltaBps,1);assert.equal(ofi.meanQuotedDirectionalDeltaBps,1);assert.equal(snap.governance.ablationSuitePreregistered,true);console.log('v0.72 ablation scorecard tests PASS');
