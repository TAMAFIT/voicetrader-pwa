import assert from 'node:assert/strict';import { createScorecardState,updateScorecardState,buildScorecardSnapshot } from '../src/short-horizon/prospective-scorecard.js';
const s=createScorecardState();for(let i=0;i<200;i++){const base={symbol:'BTC/USD',horizonSec:15,evaluationPartition:{role:'BLIND_EXAM'},provenance:{inputWindowId:`w${i}`},targetTimestampMs:i*1000,decision:'LONG'};updateScorecardState(s,{...base,outcomeId:`n${i}`,experimentId:'BOUNDARY_HASH_NULL_V1',sampleRole:'NULL',directionalReturnBps:0,result:'FLAT',quotedExecution:{quotedDirectionalReturnBps:0},quotedResult:'FLAT'});updateScorecardState(s,{...base,outcomeId:`p${i}`,experimentId:'BOUNDARY_CONCORDANCE_V1',sampleRole:'PRIMARY',directionalReturnBps:1,result:'WIN',quotedExecution:{quotedDirectionalReturnBps:0.5},quotedResult:'WIN'});}const x=buildScorecardSnapshot(s),b=x.comparisons.primaryVsNull.BLIND_EXAM;assert.equal(b.quotedStability.status,'POSITIVE_CONSISTENT');assert.equal(b.quotedStability.positiveBlocks,4);assert.equal(b.quotedStability.descriptiveOnly,true);assert.equal(x.governance.noIidSignificanceClaim,true);console.log('v0.73 temporal stability tests PASS');
