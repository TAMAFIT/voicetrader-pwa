import assert from 'node:assert/strict';import { createWindowAccumulator,addMicrostructureEvent,finalizeWindow,evaluateTimeIntegrity } from '../src/short-horizon/kraken-boundary-windows.js';
assert.equal(evaluateTimeIntegrity({count:2,sum:100,min:40,max:60,last:60}).status,'PASS');assert.equal(evaluateTimeIntegrity({count:1,sum:1300,min:1300,max:1300,last:1300}).status,'WARN');assert.equal(evaluateTimeIntegrity({count:1,sum:2500,min:2500,max:2500,last:2500}).status,'FAIL');const t=Date.parse('2026-08-22T00:00:00Z'),a=createWindowAccumulator({symbol:'BTC/USD',windowSec:1,bucketStart:t});addMicrostructureEvent(a,{eventType:'BOOK',symbol:'BTC/USD',receivedTimestampMs:t+150,providerTimestamp:new Date(t+100).toISOString(),book:{mid:100,bestBid:{price:99.9},bestAsk:{price:100.1},bidDepth:10,askDepth:10}});const w=finalizeWindow(a);assert.equal(w.timing.timeIntegrity.status,'PASS');assert.equal(w.timing.timeIntegrity.meanProviderToReceiveMs,50);assert.equal(w.timing.timeIntegrity.prospectiveEligible,true);console.log('v0.69 time integrity tests PASS');
