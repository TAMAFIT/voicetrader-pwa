import assert from 'node:assert/strict';
import { assignEvaluationPartition,attachEvaluationPartition,adaptiveLearningAllowed,buildBlindLedgerRecord } from '../src/short-horizon/blind-epoch.js';
let blindId=null,learningId=null;for(let i=0;i<100;i++){const id=`w${i}`;const role=assignEvaluationPartition(id).role;if(role==='BLIND_EXAM'&&!blindId)blindId=id;if(role==='LEARNING_STREAM'&&!learningId)learningId=id;}assert.ok(blindId&&learningId);const blindSignal=attachEvaluationPartition({signalId:'s',inputWindowId:blindId,symbol:'BTC/USD',experimentId:'BOUNDARY_ADAPTIVE_V1'});const outcome={outcomeId:'o',horizonSec:15,maturedAtMs:1,governance:{signalImmutable:true}};assert.equal(blindSignal.evaluationPartition.role,'BLIND_EXAM');assert.equal(adaptiveLearningAllowed(blindSignal,outcome),false);const ledger=buildBlindLedgerRecord(blindSignal,outcome);assert.equal(ledger.status,'RETIRED_EXPOSED');assert.equal(ledger.governance.eligibleForAdaptiveLearning,false);assert.equal(ledger.governance.eligibleForFutureBlindExam,false);const learningSignal=attachEvaluationPartition({...blindSignal,inputWindowId:learningId});assert.equal(learningSignal.evaluationPartition.role,'LEARNING_STREAM');assert.equal(adaptiveLearningAllowed(learningSignal,outcome),true);console.log('v0.60 blind epoch core tests PASS');
