import { validateModelAdapterDryRunRequest, fingerprintModelAdapterDryRunRequest } from './model-adapter-dry-run-protocol.js';
const FORBIDDEN_KEYS=new Set(['x','y','rows','row','labels','label','features','featurevalues','targets','targetvalues','predictions','prediction','metrics','results','result','trades','ohlcv','marketatdecision']);
function scan(value,path='request',errors=[]){if(Array.isArray(value)){value.forEach((item,index)=>scan(item,`${path}[${index}]`,errors));return errors;}if(!value||typeof value!=='object')return errors;for(const [key,item] of Object.entries(value)){if(FORBIDDEN_KEYS.has(key.toLowerCase()))errors.push(`adapter-dry-run-forbidden-payload-key:${path}.${key}`);scan(item,`${path}.${key}`,errors);}return errors;}
export function auditModelAdapterDryRunRequest(request){const errors=[];const base=validateModelAdapterDryRunRequest(request);if(!base.pass)errors.push(...base.errorCodes);errors.push(...scan(request));if(request?.requestFingerprint!==fingerprintModelAdapterDryRunRequest(request))errors.push('adapter-dry-run-request-fingerprint-mismatch');return {version:'model-adapter-dry-run-strict-request-audit-0.1',pass:errors.length===0,errorCount:errors.length,errorCodes:[...new Set(errors)]};}
