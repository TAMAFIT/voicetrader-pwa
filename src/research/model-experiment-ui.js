import { fetchProspectiveExperienceDocument } from './prospective-experience-remote.js';
import { evaluateLearningReadiness } from './learning-readiness-protocol.js';
import { MODEL_EXPERIMENT_REGISTRY } from './model-experiment-registry.js';
import { fetchModelExperimentLifecycleDocument } from './model-experiment-lifecycle-remote.js';

let initialized=false;
const esc=value=>String(value??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#39;');
function ensureStyle(){if(document.querySelector('link[data-model-experiment-style]'))return;const link=document.createElement('link');link.rel='stylesheet';link.href='./model-experiment.css';link.dataset.modelExperimentStyle='true';document.head.appendChild(link);}
function template(){return `<section id="modelExperimentSection" class="model-experiment" aria-labelledby="modelExperimentTitle"><div class="model-experiment-head"><div><div class="section-kicker">Future ML Contract</div><h4 id="modelExperimentTitle">Model Experiment Spec Registry</h4><p>v0.39では、FROZEN SpecからWorker Handoff、Deterministic Matrix、Cross-runtime conformance、Execution Envelope、Adapter/Environment Lock、No-Fit IPC、Research Readiness、Prospective Collection Healthまでをread-onlyの研究契約として接続しています。モデルAdapter・fit・prediction・実行許可はまだありません。</p></div><span id="modelExperimentStatus" class="model-experiment-status">LOADING</span></div><div id="modelExperimentSummary" class="model-experiment-summary"></div><div id="modelLifecycleStates" class="model-experiment-grid"></div><div id="modelFeatureSets" class="model-experiment-grid"></div><div id="modelTemplates" class="model-experiment-grid"></div><div id="modelExperimentGuardrails" class="model-experiment-guardrails"></div><div class="research-evaluation-note model-experiment-note">Execution Envelopeは将来の実行入力を固定するだけで、Result evidenceは現在EXECUTION BLOCKEDです。内部テストはpromotion evidenceではなく、将来の別Prospective confirmationが必要です。</div></section>`;}
function insert(){if(document.getElementById('modelExperimentSection'))return true;const host=document.getElementById('humanPlaybookSection');if(!host)return false;const wrap=document.createElement('div');wrap.innerHTML=template().trim();const note=host.querySelector('.human-playbook-note');if(note)host.insertBefore(wrap.firstElementChild,note);else host.appendChild(wrap.firstElementChild);return true;}
function unavailable(message){const status=document.getElementById('modelExperimentStatus'),summary=document.getElementById('modelExperimentSummary');if(status){status.className='model-experiment-status blocked';status.textContent='UNAVAILABLE';}if(summary)summary.innerHTML=`<p class="model-experiment-empty">${esc(message)}</p>`;}
function render(datasetDocument,lifecycleDocument,lifecycleError,migratedLegacyEmpty=false){const readiness=evaluateLearningReadiness(datasetDocument),status=document.getElementById('modelExperimentStatus'),summary=document.getElementById('modelExperimentSummary'),stateHost=document.getElementById('modelLifecycleStates'),featureHost=document.getElementById('modelFeatureSets'),templateHost=document.getElementById('modelTemplates'),guards=document.getElementById('modelExperimentGuardrails');if(!status||!summary||!stateHost||!featureHost||!templateHost||!guards)return;const audit=lifecycleDocument?.audit||null,counts=audit?.counts||{draft:0,frozen:0,retired:0},states=audit?.effectiveStates||[];status.className=`model-experiment-status ${audit?.pass?'locked':'blocked'}`;status.textContent=audit?.pass?'LEDGER PASS':'LEDGER UNAVAILABLE';summary.innerHTML=`<span><small>Spec Registry</small><b>${esc(MODEL_EXPERIMENT_REGISTRY.version)}</b></span><span><small>Ledger audit</small><b>${audit?.pass?'PASS':'UNAVAILABLE'}</b></span><span><small>Draft</small><b>${counts.draft}</b></span><span><small>Frozen</small><b>${counts.frozen}</b></span><span><small>Retired</small><b>${counts.retired}</b></span><span><small>BTC data gate</small><b>${readiness.gates.btcResearchModelReady?'READY':'NOT READY'}</b></span><span><small>Controlled Writer</small><b>MANUAL ONLY</b></span><span><small>Worker Handoff</small><b>READ-ONLY MANIFEST</b></span><span><small>Feature Matrix</small><b>REFERENCE READY</b></span><span><small>Cross-runtime</small><b>JS ↔ PY REF</b></span><span><small>Execution Envelope</small><b>PREPARED / BLOCKED</b></span><span><small>Result evidence</small><b>EXECUTION BLOCKED</b></span><span><small>Adapter Environment</small><b>UNINSTALLED / LOCKED</b></span><span><small>No-Fit IPC</small><b>METADATA ONLY</b></span><span><small>Research Readiness</small><b>STAGE GATED</b></span><span><small>Collection Health</small><b>READ-ONLY</b></span><span><small>Browser write</small><b>NO</b></span><span><small>Model adapter</small><b>NO</b></span><span><small>Model fit</small><b>NO</b></span><span><small>Execution</small><b>NO</b></span><span><small>Training</small><b>NO</b></span>`;stateHost.innerHTML=`<h5>Authoritative lifecycle — GitHub remote</h5>${migratedLegacyEmpty?'<p class="model-experiment-empty">Legacy empty ledger is being read through the safe migration adapter until the data branch is upgraded.</p>':''}${audit?.pass?(states.length?states.map(item=>`<article><div><b>${esc(item.experimentId)}</b><small>revision ${esc(item.revision)}</small></div><span>${esc(item.effectiveStatus)}</span><span>${item.semanticFingerprint?esc(item.semanticFingerprint.slice(-12)):item.draftFingerprint?esc(item.draftFingerprint.slice(-12)):'draft'}</span><small>DRAFT/FROZEN snapshot mutation = forbidden</small></article>`).join(''):'<p class="model-experiment-empty">No lifecycle events yet. Empty audited ledger is valid.</p>'):`<p class="model-experiment-empty">${esc(lifecycleError||'Lifecycle ledger unavailable')}</p>`}`;featureHost.innerHTML=`<h5>Approved input-only Feature Sets</h5>${Object.values(MODEL_EXPERIMENT_REGISTRY.featureSets).map(f=>`<article><div><b>${esc(f.label)}</b><small>${esc(f.id)}</small></div><span>${f.paths.length} paths</span><span>${esc(f.supportedInstruments.join(' / '))}</span><small>learned selection = ${f.learnedSelection?'true':'false'}</small></article>`).join('')}`;templateHost.innerHTML=`<h5>Non-executable Spec Templates</h5>${MODEL_EXPERIMENT_REGISTRY.templates.map(t=>`<article><div><b>${esc(t.label)}</b><small>${esc(t.templateId)}</small></div><span>${esc(t.targetId)}</span><span>${esc(t.algorithmId)}</span><small>${esc(t.preprocessingId)} / executable = false</small></article>`).join('')}`;guards.innerHTML=`<b>Controlled registration → reproducibility → blocked execution boundary</b><span>① Experience audit PASS</span><span>② Readiness READY</span><span>③ Exact Dataset commit + cutoff</span><span>④ Human-approved active FROZEN Spec</span><span>⑤ Frozen preprocessing + rounding semantics</span><span>⑥ Exact frozen Code commit</span><span>⑦ Chronological split + embargo</span><span>⑧ Feature materialization audit</span><span>⑨ Deterministic X/y bundle</span><span>⑩ JS reference wire</span><span>⑪ Python stdlib independent wire</span><span>⑫ Byte-identical wire + fingerprint</span><span>⑬ Immutable Execution Envelope</span><span>⑭ Adapter / Dependency / Environment Lock prerequisites</span><span>⑮ No-Fit metadata IPC handshake</span><span>⑯ Separate future execution authority</span><strong>PWA/Registry/Ledger/Controlled Writer/Handoff/Matrix/Conformance/Envelope/Readinessのいずれも学習Jobを起動しません。 Registry and Controlled Writer never launch jobs.</strong>`;}
async function load(){const [experience,lifecycle]=await Promise.all([fetchProspectiveExperienceDocument(),fetchModelExperimentLifecycleDocument()]);if(!experience.document){unavailable(experience.error||'Experience Dataset unavailable');return;}render(experience.document,lifecycle.document,lifecycle.error,lifecycle.migratedLegacyEmpty);}
export function setupModelExperimentUI(){if(initialized)return;initialized=true;ensureStyle();if(!insert()){setTimeout(()=>{if(insert())load();},0);return;}load();}
