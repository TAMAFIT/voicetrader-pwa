export const SHORT_HORIZON_HUMAN_CANON_REGISTRY_VERSION = 'short-horizon-human-canon-registry-v1';

const frozen = (value) => Object.freeze(value);

export const SHORT_HORIZON_HUMAN_CANON_REGISTRY = frozen({
  version: SHORT_HORIZON_HUMAN_CANON_REGISTRY_VERSION,
  role: 'frozen-human-canon-benchmark',
  supportedTimeframesMinutes: frozen([1, 5]),
  philosophy: frozen({
    textbookCanonicalBaseline: true,
    profitabilityClaim: false,
    calibratedProbability: false,
    expectedReturnModel: false,
    optimizedOnObservedShortHorizonData: false,
    parameterSweep: false,
    adaptiveWeights: false,
    selfLearning: false,
    automaticPromotion: false,
    equalFamilyWeight: true,
    correlatedRuleCountDoesNotIncreaseFamilyWeight: true,
    executionAuthorized: false,
  }),
  horizons: frozen({
    1: frozen({ primaryMinutes:5, secondaryMinutes:15 }),
    5: frozen({ primaryMinutes:15, secondaryMinutes:30 }),
  }),
  indicatorPeriods: frozen({
    fastSma:12,
    slowSma:34,
    rsi:14,
    atr:14,
    macd: frozen({ fast:12, slow:26, signal:9 }),
    bollinger: frozen({ length:20, deviations:2 }),
    stochastic:14,
    dmiAdx:14,
    donchian:20,
    shortRoc:6,
    mediumRoc:24,
    structure:8,
    obvSlope:10,
    volumeZ:30,
  }),
  thresholds: frozen({
    signalComposite:25,
    minimumFamilyAgreement:0.60,
    minimumDirectionalFamilies:3,
    familyMeaningfulScore:15,
    rsiMomentumHigh:55,
    rsiMomentumLow:45,
    rsiExtremeHigh:70,
    rsiExtremeLow:30,
    stochasticMomentumHigh:55,
    stochasticMomentumLow:45,
    stochasticExtremeHigh:80,
    stochasticExtremeLow:20,
    bollingerExtremeZ:1.5,
    adxTrend:25,
    structureDirectional:0.25,
    obvDirectional:0.15,
    volumeSpikeZ:1.0,
    compressionPercentile:20,
    volatilePercentile:85,
    cautionVolatilityPercentile:90,
    extremeVolatilityPercentile:97,
    recentContinuityBars:34,
  }),
  families: frozen([
    frozen({ id:'trend', role:'directional', equalWeight:true }),
    frozen({ id:'momentum', role:'directional', equalWeight:true }),
    frozen({ id:'meanReversion', role:'directional', equalWeight:true }),
    frozen({ id:'structure', role:'directional', equalWeight:true }),
    frozen({ id:'participation', role:'directional-context', equalWeight:true }),
  ]),
  canonicalConcepts: frozen([
    'moving-average alignment and slope',
    'MACD 12/26/9 direction',
    'DMI/ADX trend confirmation',
    'RSI 14 momentum and 30/70 extremes',
    'Stochastic 14 momentum and 20/80 extremes',
    'Bollinger 20/2 distribution stretch',
    'Donchian 20 breakout',
    'HH/HL versus LH/LL structure',
    'OBV direction and relative volume spike confirmation',
  ]),
});

export function getShortHorizonHumanCanonRegistrySnapshot() {
  return JSON.parse(JSON.stringify(SHORT_HORIZON_HUMAN_CANON_REGISTRY));
}
