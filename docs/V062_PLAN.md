v0.62 plan: closed-hour Kraken raw audit over RFC7464 wire + NDJSON meta. Verify one-to-one record alignment, per-message SHA-256 and byte length, monotonic local sequence within each connection, parseability and closed-hour immutability assumption. Persist audit evidence locally. Audit never repairs or rewrites raw data. FAIL means affected hour is not trusted for finalized research evidence. No Google Cloud or GitHub Actions runtime dependency.